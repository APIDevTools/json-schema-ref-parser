title: Person
type: object
required:
  - name
properties:
  name:
    $ref: "#/definitions/name"
  age:
    type: integer
    minimum: 0
  gender:
    type: string
    enum:
      - male
      - female
definitions:
  $ref: definitions/definitions.json
